diff --git a/.gitignore b/.gitignore
index 5f45b5d..3c937e7 100644
--- a/.gitignore
+++ b/.gitignore
@@ -21,6 +21,7 @@
 *.i*86
 *.x86_64
 *.hex
+/src/alock
 
 # Autotools
 /autom4te.cache/
@@ -34,6 +35,13 @@
 Makefile.in
 config.h.in
 *~
+/Makefile
+/config.h
+/config.status
+/stamp-h1
+/doc/Makefile
+/src/Makefile
+/src/.deps/
 
 *.d
 config.mk
diff --git a/src/main.c b/src/main.c
index e4cb32e..56e8c10 100644
--- a/src/main.c
+++ b/src/main.c
@@ -317,6 +317,27 @@ static int lockDisplay(Display *display, struct aModules *modules) {
     return 0;
 }
 
+static void refreshCursorAfterResume(Display *display, struct aModules *modules,
+                                     Cursor blank_cursor, Cursor argb_cursor) {
+    int nscreens = ScreenCount(display);
+
+    XChangeActivePointerGrab(display, 0, blank_cursor, CurrentTime);
+
+    for (int i = 0; i < nscreens; i++) {
+        Window bg = modules->background->getwindow(i);
+        if (bg != None) {
+            XRaiseWindow(display, bg);
+            XClearWindow(display, bg);
+        }
+    }
+
+    XSync(display, False);
+    usleep(500000);
+
+    XChangeActivePointerGrab(display, 0, argb_cursor, CurrentTime);
+    XSync(display, False);
+}
+
 static void eventLoop(Display *display, struct aModules *modules) {
 
     XEvent ev;
@@ -326,10 +347,22 @@ static void eventLoop(Display *display, struct aModules *modules) {
     unsigned int clen;
     unsigned int pass_pos = 0, pass_len = 0;
     unsigned long keypress_time = 0;
+    unsigned long last_time = alock_mtime();
 
     /* if possible do not page this address to the swap area */
     mlock(pass, sizeof(pass));
 
+    /* blank cursor held during the post-resume repaint window */
+    Cursor blank_cursor;
+    {
+        Window root = DefaultRootWindow(display);
+        Pixmap p = XCreatePixmap(display, root, 1, 1, 1);
+        XColor black = {0};
+        blank_cursor = XCreatePixmapCursor(display, p, p, &black, &black, 0, 0);
+        XFreePixmap(display, p);
+    }
+    Cursor argb_cursor = modules->cursor->getcursor();
+
     debug("entering event main loop");
     for (;;) {
 
@@ -343,6 +376,11 @@ static void eventLoop(Display *display, struct aModules *modules) {
                     keypress_time = 0;
                 }
 
+                unsigned long now = alock_mtime();
+                if (now - last_time > 500)
+                    refreshCursorAfterResume(display, modules, blank_cursor, argb_cursor);
+                last_time = now;
+
                 /* wait a bit */
                 usleep(25000);
                 continue;
@@ -356,8 +394,14 @@ static void eventLoop(Display *display, struct aModules *modules) {
                 setBacklightBrightness(0);
 #endif /* WITH_XBLIGHT */
 
-            /* block until any key press event arrives */
-            XMaskEvent(display, KeyPressMask | StructureNotifyMask, &ev);
+            /* Poll for events so resume can be detected via clock jump */
+            while (XCheckMaskEvent(display, KeyPressMask | StructureNotifyMask, &ev) == False) {
+                unsigned long now = alock_mtime();
+                if (now - last_time > 500)
+                    refreshCursorAfterResume(display, modules, blank_cursor, argb_cursor);
+                last_time = now;
+                usleep(100000);
+            }
 
 #if WITH_XBLIGHT
             /* restore original backlight brightness value */
@@ -366,6 +410,8 @@ static void eventLoop(Display *display, struct aModules *modules) {
 #endif /* WITH_XBLIGHT */
         }
 
+        last_time = alock_mtime();
+
         switch (ev.type) {
         case KeyPress:
 
