diff --git a/.gitignore b/.gitignore
index 5f45b5d..1333aab 100644
--- a/.gitignore
+++ b/.gitignore
@@ -38,3 +38,13 @@ config.h.in
 *.d
 config.mk
 config.log
+
+# Build outputs
+/Makefile
+/config.h
+/config.status
+/stamp-h1
+doc/Makefile
+src/Makefile
+src/.deps/
+src/alock
diff --git a/configure.ac b/configure.ac
index d567372..1654c45 100644
--- a/configure.ac
+++ b/configure.ac
@@ -21,6 +21,8 @@ AM_COND_IF([ENABLE_DEBUG], [
 
 AC_SEARCH_LIBS([clock_gettime], [rt])
 PKG_CHECK_MODULES([X11], [x11])
+PKG_CHECK_MODULES([XFIXES], [xfixes])
+PKG_CHECK_MODULES([XRANDR], [xrandr])
 
 # check for the Misc X Extension library
 PKG_CHECK_MODULES([XEXT], [xext],
diff --git a/src/Makefile.am b/src/Makefile.am
index f81d4dd..f60553c 100644
--- a/src/Makefile.am
+++ b/src/Makefile.am
@@ -21,7 +21,9 @@ alock_CFLAGS = \
 	@XEXT_CFLAGS@ \
 	@XPM_CFLAGS@ \
 	@XRENDER_CFLAGS@ \
-	@IMLIB2_CFLAGS@
+	@IMLIB2_CFLAGS@ \
+	@XFIXES_CFLAGS@ \
+	@XRANDR_CFLAGS@
 
 alock_LDADD = \
 	@X11_LIBS@ \
@@ -29,7 +31,9 @@ alock_LDADD = \
 	@XEXT_LIBS@ \
 	@XPM_LIBS@ \
 	@XRENDER_LIBS@ \
-	@IMLIB2_LIBS@
+	@IMLIB2_LIBS@ \
+	@XFIXES_LIBS@ \
+	@XRANDR_LIBS@
 
 if ENABLE_PAM
 alock_SOURCES += auth_pam.c
diff --git a/src/main.c b/src/main.c
index e4cb32e..7d23fe5 100644
--- a/src/main.c
+++ b/src/main.c
@@ -11,12 +11,13 @@
 
 #include "alock.h"
 
+#include <X11/extensions/Xfixes.h>
+#include <X11/extensions/Xrandr.h>
 #include <ctype.h>
 #include <getopt.h>
 #include <locale.h>
 #include <signal.h>
 #include <spawn.h>
-#include <stdbool.h>
 #include <stdlib.h>
 #include <string.h>
 #include <unistd.h>
@@ -175,6 +176,38 @@ static void setBacklightBrightness(float value) {
 }
 #endif /* WITH_XBLIGHT */
 
+/* Polls up to timeout_ms for Expose/VisibilityNotify from any of wins[]. */
+static void waitForPaint(Display *display, Window *wins, int nwins,
+                         unsigned long timeout_ms) {
+
+    unsigned long start = alock_mtime();
+
+    while (alock_mtime() - start < timeout_ms) {
+
+        XEvent ev;
+
+        while (XCheckMaskEvent(display,
+                    ExposureMask | StructureNotifyMask | VisibilityChangeMask,
+                    &ev)) {
+
+            Window src = None;
+            if (ev.type == Expose)
+                src = ev.xexpose.window;
+            else if (ev.type == VisibilityNotify)
+                src = ev.xvisibility.window;
+            else
+                /* drain StructureNotify; not relevant to paint state */
+                continue;
+
+            for (int j = 0; j < nwins; j++)
+                if (wins[j] == src)
+                    return;
+        }
+
+        usleep(10000);
+    }
+}
+
 /* Lock current display and grab pointer and keyboard. On successful
  * lock this function returns 0, otherwise -1. */
 static int lockDisplay(Display *display, struct aModules *modules) {
@@ -221,65 +254,11 @@ static int lockDisplay(Display *display, struct aModules *modules) {
      */
     XSync(display, False);
 
-    if (bgcount > 0) {
+    if (bgcount > 0)
+        waitForPaint(display, bgwins, bgcount, 500);
 
-        unsigned long start = alock_mtime();
-        bool painted = false;
-
-        /*
-         * Upper bound to avoid blocking forever; X11 gives no guarantee that
-         * expose or visibility events will ever arrive.
-         */
-        while (alock_mtime() - start < 500) {
-
-            XEvent ev;
-
-            /* Pump events; do not block hard forever */
-            while (XCheckMaskEvent(display,
-                        ExposureMask | StructureNotifyMask | VisibilityChangeMask,
-                        &ev)) {
-
-                /* Only accept events from one of our background windows */
-                Window src = None;
-                if (ev.type == Expose)
-                    src = ev.xexpose.window;
-                else if (ev.type == VisibilityNotify)
-                    src = ev.xvisibility.window;
-                else
-                    /*
-                    * Intentionally drain non-paint-related events (e.g. StructureNotify)
-                    * from the queue; they are not relevant to our painting check.
-                    */
-                    continue;
-
-                /*
-                * Only treat paint-related events from our own background windows as
-                * meaningful; X11 may deliver expose/visibility events for unrelated
-                * windows (e.g. root or WM-managed windows).
-                */
-                for (int j = 0; j < bgcount; j++)
-                    if (bgwins[j] == src) {
-                        painted = true;
-                        break;
-                    }
-
-                if (painted)
-                    break;
-            }
-
-            if (painted)
-                break;
-
-            /*
-             * Yield briefly to let the server/compositor make progress without
-             * busy-spinning while waiting for paint-related events.
-             */
-            usleep(10000);
-        }
-
-        /* Final sync before cursor/grab */
-        XSync(display, False);
-    }
+    /* Final sync before cursor/grab */
+    XSync(display, False);
 
     free(bgwins);
 
@@ -317,6 +296,79 @@ static int lockDisplay(Display *display, struct aModules *modules) {
     return 0;
 }
 
+/* On resume from suspend, hide the cursor via XFixes, reinitialize the cursor
+ * hardware plane via an XRandR same-mode modeset, repaint all background
+ * windows, wait for the repaint to land, then show the cursor again.
+ * Modeset order matters: the hardware reinit must happen while the cursor is
+ * hidden and before the clean framebuffer is presented, so ShowCursor forces
+ * the driver to read fresh pixels rather than any cached sprite backing. */
+static void refreshCursorAfterResume(Display *display, struct aModules *modules) {
+
+    int nscreens = ScreenCount(display);
+    Window bgwins[nscreens];
+    int bgcount = 0;
+
+    for (int i = 0; i < nscreens; i++)
+        XFixesHideCursor(display, RootWindow(display, i));
+
+    /* Reinit cursor hardware plane while cursor is hidden and framebuffer may
+     * still be stale — the driver has no cached sprite to reuse after this. */
+    for (int i = 0; i < nscreens; i++) {
+        Window root = RootWindow(display, i);
+        XRRScreenResources *rr = XRRGetScreenResourcesCurrent(display, root);
+        if (!rr) continue;
+        for (int c = 0; c < rr->ncrtc; c++) {
+            XRRCrtcInfo *ci = XRRGetCrtcInfo(display, rr, rr->crtcs[c]);
+            if (ci && ci->mode != None)
+                XRRSetCrtcConfig(display, rr, rr->crtcs[c], CurrentTime,
+                        ci->x, ci->y, ci->mode, ci->rotation,
+                        ci->outputs, ci->noutput);
+            if (ci) XRRFreeCrtcInfo(ci);
+        }
+        XRRFreeScreenResources(rr);
+    }
+    XSync(display, False);
+
+    /* Discard stale paint events (including those from the modeset) so
+     * waitForPaint sees only the fresh ones from the XClearArea calls below. */
+    {
+        XEvent drain_ev;
+        while (XCheckMaskEvent(display, ExposureMask | VisibilityChangeMask, &drain_ev))
+            ;
+    }
+
+    for (int i = 0; i < nscreens; i++) {
+        Window bg = modules->background->getwindow(i);
+        if (bg != None) {
+            XRaiseWindow(display, bg);
+            /* XClearArea with exposures=True generates an Expose event for waitForPaint. */
+            XClearArea(display, bg, 0, 0, 0, 0, True);
+            bgwins[bgcount++] = bg;
+        }
+    }
+
+    XSync(display, False);
+
+    if (bgcount > 0) {
+        waitForPaint(display, bgwins, bgcount, 1000);
+        /* GPU fence: forces the server to finish rendering before we restore the cursor. */
+        XImage *img = XGetImage(display, bgwins[0], 0, 0, 1, 1, AllPlanes, ZPixmap);
+        if (img) XDestroyImage(img);
+    } else {
+        usleep(500000);
+    }
+
+    /* GPU scanout settle time. */
+    usleep(100000);
+
+    /* Restore cursor: driver must re-read the framebuffer post-modeset reinit,
+     * which now contains clean lock-screen pixels. */
+    for (int i = 0; i < nscreens; i++)
+        XFixesShowCursor(display, RootWindow(display, i));
+
+    XSync(display, False);
+}
+
 static void eventLoop(Display *display, struct aModules *modules) {
 
     XEvent ev;
@@ -326,6 +378,8 @@ static void eventLoop(Display *display, struct aModules *modules) {
     unsigned int clen;
     unsigned int pass_pos = 0, pass_len = 0;
     unsigned long keypress_time = 0;
+    unsigned long last_time = alock_mtime();
+    unsigned long deferred_refresh_at = 0;
 
     /* if possible do not page this address to the swap area */
     mlock(pass, sizeof(pass));
@@ -343,6 +397,25 @@ static void eventLoop(Display *display, struct aModules *modules) {
                     keypress_time = 0;
                 }
 
+                unsigned long now = alock_mtime();
+                if (now - last_time > 500) {
+                    /* Resume detected: hide immediately so no artifact is visible
+                     * while the display finishes reinitializing. The full refresh
+                     * is deferred 1500 ms so the NVIDIA driver has time to complete
+                     * its own VRAM restore before we repaint and show the cursor. */
+                    int ns = ScreenCount(display);
+                    for (int i = 0; i < ns; i++)
+                        XFixesHideCursor(display, RootWindow(display, i));
+                    deferred_refresh_at = alock_mtime() + 1500;
+                }
+                last_time = alock_mtime();
+
+                if (deferred_refresh_at && alock_mtime() >= deferred_refresh_at) {
+                    refreshCursorAfterResume(display, modules);
+                    deferred_refresh_at = 0;
+                    last_time = alock_mtime();
+                }
+
                 /* wait a bit */
                 usleep(25000);
                 continue;
@@ -356,8 +429,25 @@ static void eventLoop(Display *display, struct aModules *modules) {
                 setBacklightBrightness(0);
 #endif /* WITH_XBLIGHT */
 
-            /* block until any key press event arrives */
-            XMaskEvent(display, KeyPressMask | StructureNotifyMask, &ev);
+            /* poll for events so resume can be detected via clock jump */
+            while (XCheckMaskEvent(display, KeyPressMask | StructureNotifyMask, &ev) == False) {
+                unsigned long now = alock_mtime();
+                if (now - last_time > 500) {
+                    int ns = ScreenCount(display);
+                    for (int i = 0; i < ns; i++)
+                        XFixesHideCursor(display, RootWindow(display, i));
+                    deferred_refresh_at = alock_mtime() + 1500;
+                }
+                last_time = alock_mtime();
+
+                if (deferred_refresh_at && alock_mtime() >= deferred_refresh_at) {
+                    refreshCursorAfterResume(display, modules);
+                    deferred_refresh_at = 0;
+                    last_time = alock_mtime();
+                }
+
+                usleep(100000);
+            }
 
 #if WITH_XBLIGHT
             /* restore original backlight brightness value */
@@ -366,6 +456,8 @@ static void eventLoop(Display *display, struct aModules *modules) {
 #endif /* WITH_XBLIGHT */
         }
 
+        last_time = alock_mtime();
+
         switch (ev.type) {
         case KeyPress:
 
